# Tuples

A tuple is a collection where each field may have a different type. Tuples can
be created using standard Rust tuple syntax.

## Supported Operations

- Comparison operators: `==`, `!=`, `<`, `>`, `<=`, `>=`
- Field access: `.0`, `.1`... `.<n-1>`

## Pasting

Tuples currently cannot be pasted.
